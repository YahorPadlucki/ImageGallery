package signals
{
import mx.collections.ArrayCollection;

import org.osflash.signals.Signal;

public class ImagesLoadingCompleteSignal  extends Signal
{
    public function ImagesLoadingCompleteSignal()
    {
        super (ArrayCollection)
    }
}
}
